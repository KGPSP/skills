import { defineConfig, devices } from '@playwright/test';

/**
 * Playwright config — generated by playwright-test-suite skill.
 * Optimized for: evidence generation, cross-browser, CI-friendly.
 *
 * Override w playwright.config.local.ts jeśli potrzebujesz.
 */
export default defineConfig({
  testDir: './tests',
  testMatch: ['**/*.spec.ts', '**/generated/**/*.spec.ts'],

  // Parallelization
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,

  // Reporters — JSON for parsing, HTML for human review
  reporter: [
    ['html', { outputFolder: 'playwright-report', open: 'never' }],
    ['json', { outputFile: 'tests-results.json' }],
    ['list'],
  ],

  // Evidence collection — always on
  use: {
    baseURL: process.env.APP_URL || 'http://localhost:3000',
    trace: 'on',                  // FULL trace dla evidence (przesłaniaj na 'on-first-retry' jeśli za ciężkie)
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
    actionTimeout: 10_000,
    navigationTimeout: 30_000,
  },

  // Snapshots — visual regression
  expect: {
    toHaveScreenshot: {
      maxDiffPixels: 100,
      threshold: 0.1,
    },
  },

  // Projects — cross-browser
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
    // Mobile (opcjonalne — odkomentuj jeśli aplikacja mobile)
    // { name: 'mobile-chrome', use: { ...devices['Pixel 5'] } },
    // { name: 'mobile-safari', use: { ...devices['iPhone 13'] } },
  ],

  // Auto-start app server during tests (lokalne tylko)
  webServer: process.env.CI ? undefined : {
    command: process.env.APP_START_CMD || 'npm run dev',
    url: process.env.APP_URL || 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
});
