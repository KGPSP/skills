import type { Config } from 'jest'
import nextJest from 'next/jest.js'

const createJestConfig = nextJest({ dir: './' })

const config: Config = {
  coverageProvider: 'v8',
  testEnvironment: 'jsdom',
  setupFilesAfterEach: ['<rootDir>/src/test/setup-jest.ts'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1'
  },
  collectCoverage: false,
  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/test/**',
    '!**/*.d.ts',
    '!**/*.config.{ts,js}'
  ],
  coverageReporters: ['text', 'lcov', 'json-summary'],
  coverageThreshold: {
    global: {
      lines: {{COVERAGE_LINES}},
      functions: {{COVERAGE_FUNCTIONS}},
      branches: {{COVERAGE_BRANCHES}},
      statements: {{COVERAGE_STATEMENTS}}
    }
  },
  testMatch: [
    '<rootDir>/src/**/*.test.{ts,tsx}',
    '<rootDir>/tests/integration/**/*.{test,int.test}.ts'
  ],
  testPathIgnorePatterns: ['/node_modules/', '/.next/', '/tests/e2e/'],
  reporters: [
    'default',
    ['jest-junit', { outputDirectory: './reports', outputName: 'jest.junit.xml' }]
  ]
}

export default createJestConfig(config)
