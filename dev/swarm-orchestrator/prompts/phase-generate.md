# PHASE: generate

Workspace: `{{WORKSPACE}}`
Run id: `{{RUN_ID}}`
Sprint: `{{SPRINT}}`
Contract: `{{STATE_DIR}}/contracts/sprint-{{SPRINT}}.json`

Implement the smallest useful slice that satisfies the contract. Write or update
tests before product code when feasible. Do not mark criteria passed and do not
write evaluator evidence.

When blocked, write a short blocker note to `{{STATE_DIR}}/messages/`.
